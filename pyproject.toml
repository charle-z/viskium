[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[project]
name = "viskium"
dynamic = ["version"]
description = "A resource-bounded, local-first foundation for temporal capture and structured observations."
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = []
authors = [{ name = "Carlos Acosta" }]
license = "Apache-2.0"
license-files = ["LICENSE"]
keywords = ["computer-vision", "local-first", "resource-bounded"]
classifiers = [
  "Development Status :: 2 - Pre-Alpha",
  "Operating System :: Microsoft :: Windows",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.13",
  "Typing :: Typed",
]

[project.optional-dependencies]
agent = [
  "anyio>=4.5,<5",
  "mcp==2.1.1",
  "pydantic>=2.11,<3",
]
camera = ["opencv-python-headless>=5,<6"]

[project.scripts]
viskium = "viskium.cli:main"

[project.urls]
Homepage = "https://github.com/charle-z/viskium"
Repository = "https://github.com/charle-z/viskium"
Issues = "https://github.com/charle-z/viskium/issues"
Security = "https://github.com/charle-z/viskium/security/advisories"

[tool.hatch.version]
path = "src/viskium/_version.py"

[dependency-groups]
dev = [
  "build>=1.2,<2",
  "hatchling==1.32.0",
  "hypothesis>=6.130,<7",
  "mypy>=1.16,<2",
  "pytest>=8.3,<10",
  "pytest-cov>=6,<8",
  "ruff>=0.12,<1",
]

[tool.hatch.build.targets.sdist]
include = [
  "/.agents",
  "/.codex",
  "/.python-version",
  "/scripts",
  "/src/viskium",
  "/tests",
  "/docs",
  "/LICENSE",
  "/README.md",
  "/CONTRIBUTING.md",
  "/SECURITY.md",
  "/uv.lock",
]
exclude = ["/docs/plans"]

[tool.hatch.build.targets.wheel]
packages = ["src/viskium"]

[tool.uv]
required-version = "==0.12.7"

[tool.ruff]
target-version = "py313"
line-length = 100
src = ["src", "tests"]
extend-exclude = [
  ".extras-wheel-venv",
  ".uv-cache",
  ".venv",
  ".wheel-venv",
  "build",
  "dist",
]

[tool.ruff.lint]
select = [
  "B",
  "C4",
  "E4",
  "E7",
  "E9",
  "F",
  "I",
  "PIE",
  "PT",
  "RUF",
  "SIM",
  "UP",
]

[tool.ruff.lint.isort]
known-first-party = ["viskium"]

[tool.ruff.format]
docstring-code-format = true
line-ending = "lf"

[tool.mypy]
python_version = "3.13"
files = ["src/viskium"]
strict = true
pretty = true
show_error_codes = true
warn_unused_configs = true

[tool.pytest.ini_options]
minversion = "8.3"
testpaths = ["tests"]
addopts = [
  "-ra",
  "--strict-config",
  "--strict-markers",
  "--cov=viskium",
  "--cov-branch",
  "--cov-report=term-missing:skip-covered",
  "--cov-fail-under=90",
  "-m",
  "not hardware",
]
xfail_strict = true
markers = [
  "hardware: requires explicitly authorized physical hardware access",
]

[tool.coverage.run]
branch = true
source = ["viskium"]

[tool.coverage.report]
fail_under = 90
precision = 1
show_missing = true
skip_covered = true
exclude_also = [
  "if TYPE_CHECKING:",
]
